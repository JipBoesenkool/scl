#ifndef C_LINKED_LIST_H
#define C_LINKED_LIST_H

/*
// TODO: explore this party trick for entity ids
typedef struct {
    uintptr_t* xor;
} Node;

typedef struct {
    Node* begin;
    Node* end;
} Linked_List;
// a^a^b = b
// a^b^b = a
*/

#endif // C_LINKED_LIST_H
